import { FireOutlined } from '@ant-design/icons';
import { type MenuItem } from '@shared/components/Sidebar';

export const dishMeta: MenuItem[] = [
  {
    key: '/dish',
    label: '菜品制作',
    icon: <FireOutlined />,
  },
];
